import { createRoot } from "react-dom/client";
import ClimbDeezWebAppContainer from "./src/App.js";

const root = createRoot(document.getElementById("root"));
root.render(<ClimbDeezWebAppContainer />);
